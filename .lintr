linters:
    with_defaults(
        line_length_linter(120),
        assignment_linter = NULL,
        single_quotes_linter = NULL,
        object_usage_linter = NULL
        )
exclusions:
    list('R/sysdata.R',
         'R/test-data.R')
